# Premier Rheumatology — Weekly Digest (Week of Jun 01 – Jun 07, 2026)

site: premierjointcare.com · Boca Raton, FL + Queens, NY · providers: Dr. Mouriel Boucher (DO), Natalie Akerman (NP), Dr. Bruce Stein (MD)
Compiled: 2026-06-08 (Monday). Covers daily notes 06-01 → 06-07 and `git log` on `main` for the trailing 7 days.

> **Sync note:** Remote git is unreachable from the agent session (`git fetch/pull/push` → "Host key verification failed" — no SSH key/known_hosts in-session). This digest was produced against the **local** repo. Local `main` is ≥10 commits ahead of `origin/main` and nothing has deployed this week. This is WO-012 and gates everything below.

## What moved this week

**Rankings — flat, no boundary crossings.** premierjointcare.com stayed pinned at **0/10** on all priority non-brand queries every pass, Jun 1 → Jun 7 (rheumatologist / lupus / RA / osteoporosis / arthritis / autoimmune / biologic infusion / psoriatic / gout — Boca Raton; rheumatologist Queens NY). No keyword entered or left page 1. Brand term "Premier Rheumatology Boca Raton" held **#1** all week — but the indexed result still shows the **legacy WordPress title** ("Florida's Top Joint & Autoimmune Care"), legacy URLs, and stale old phone/fax (440-3270 / 440-3280) → WO-008. The 45 condition pages remain effectively self-deindexed (clean-URL canonicals 404).

**AI visibility — flat at 0.** Daily probe scores were 0/4–0/5 every pass all week; Premier / Boucher / Dr. Bruce Stein cited in **zero** Boca or Queens AI answers. The dominant story: **Dr. Bruce Stein absent from the "best rheumatologist Queens NY" recommendation set for 12 consecutive passes** as of Sun evening (was 4 at the start of the week). Root cause confirmed by directory sweep — Stein's entire web presence is Floral-Park-branded; no standalone Premier Queens profile exists anywhere (WO-003 Part A). One soft-positive blip: a 06-07 AM infusion probe surfaced Premier (1/4), **not repeatable** on re-probe — logged as a one-off.

**Site health — green, with one confirmed live defect.** Apex verified healthy via live fetches (307→www→200 on Vercel, correct title/phone/providers/footer). The long-running "apex reverted to WordPress" scare (**WO-013**) was re-adjudicated against the live site and **closed as DISPROVEN/false-alarm** (root cause = stale Google snippet = WO-008). Real defect, now sharply framed: the live **`sitemap.xml` serves 6×404 to crawlers** — 6 of 7 submitted extensionless URLs (`/about`, `/florida`, `/new-york`, `/team`, `/conditions`, `/contact`) return 404 in production while their `.html` forms return 200. Active crawl waste = WO-008.

## What I shipped autonomously (committed locally; awaiting push via WO-012)

- **Metadata:** Completed Twitter Card markup (`twitter:title` + `twitter:description`) on the 7 main pages, matching the 45 condition pages — site-wide on-page hygiene now at **0 gaps** (verified twice).
- **Schema (WO-011):** Implemented location-page FAQ blocks + `FAQPage` and `Physician` JSON-LD on `florida.html` and `new-york.html`; SSR-verified in a clean build.
- **Copy / NAP:** Fixed Coconut Creek suite number **234 → 236** sitewide.
- **Work orders / docs:** Committed WO-013 as a CLOSED/DISPROVEN record so it stops reopening; logged daily notes + a WO-008 re-confirmation note. No new WO drafted this week (existing coverage adequate; tracking two new names — thehirshcenter.com, usasportsmedicine.com — sub-WO watch).
- **Held back (deliberate):** `vercel.json` WO-008 `cleanUrls` candidate left **uncommitted** — a site-wide routing change that needs a human Vercel preview test before deploy.

## What needs Yonah's attention

1. **Restore push auth (WO-012).** This is the gate on everything — local `main` is ≥10 commits ahead and nothing this week (Twitter Cards, WO-011 schema, suite fix) has deployed. Add a deploy SSH key + known_hosts (or switch `origin` to authenticated HTTPS token), then push.
2. **Test + ship the `vercel.json` WO-008 fix.** #1 SEO blocker. Verify on a Vercel preview that `/about`, `/conditions`, `/florida`, `/new-york`, `/team`, `/contact`, `/conditions/<slug>` return 200 (plus `/api/contact` spot-check), then merge. Until clean URLs serve 200, condition pages stay self-deindexed and the stale legacy snippet persists in the brand SERP.
3. **WO-003 Part A — Queens directory consolidation (front-desk).** Most urgent off-site item: rebrand Dr. Bruce Stein's directory profiles (Healthgrades / Zocdoc / US News / WebMD / Doximity) from Floral Park → Premier Rheumatology / Queens. This is what's driving the 12-pass Queens recommendation regression. Pair with WO-010 (Certainly Health).
4. **Other front-desk WOs awaiting approval:** WO-006 (Boucher South Miami → Boca), WO-005 (Boca Observer / Tebra / Medifind + Richard-vs-Bruce Stein disambiguation), WO-009 (RateMDs / Practo).

## Next week's focus

1. **Get the deploy pipe open (WO-012)** so the week's shipped work actually goes live — then validate `/florida` + `/new-york` in Google Rich Results Test.
2. **Ship WO-008** (clean URLs) and follow immediately with WO-007 (sitemap 7→52 URLs) to accelerate re-crawl.
3. **Keep monitoring the Queens/Stein regression and the infusion AI probe** (both phrasings) for repeatability; watch thehirshcenter.com climb.
